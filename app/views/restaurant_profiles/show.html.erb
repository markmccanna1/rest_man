<h1><%= @restaurant.menus.first.title %></h1>

<div data-role="page" id="page2">
    <div data-role="content">
        <div data-role="collapsible-set">
            <div data-role="collapsible" data-collapsed="false">
                <h3>
                    <%= @restaurant.menus.first.categories.first.title %>
                </h3>
                <ul>
                    <% @restaurant.menus.first.categories.first.menu_items.each do |item| %>
                    <li><%= item.name %></li>
                    <p><%= item.description %></p>
                    <p><%= item.price %></p>
                    <a href="#confirm" class="order_confirmation" data-rel="popup" id="order_confirm">Order this Item</a>
                  <!--   <div data-role="popup" id="confirm">
                        <form action="">
                            <p>Are you sure you want to order this item?</p>
                            <input type="submit" value="Yes">
                            <input type="submit" value="No">
                        </form>
                    </div> -->
                    <% end %>
                </ul>
            </div>
            <div data-role="collapsible" data-collapsed="false">
                <h3>
                    Section Header
                </h3>
            </div>
            <div data-role="collapsible" data-collapsed="false">
                <h3>
                    Section Header
                </h3>
            </div>
            <div data-role="collapsible" data-collapsed="false">
                <h3>
                    Section Header
                </h3>
            </div>
        </div>
    </div>
</div>
